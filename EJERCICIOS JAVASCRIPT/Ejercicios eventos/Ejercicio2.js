function dragstart(capa1){
    thus.style.opacity = '0.5';
}
var capes = document.querySelectorAll('#capa1');
[].forEach.call(capes, function(capa) {
    capa.addEventListener('dragstart', dragstart, false);
};
    